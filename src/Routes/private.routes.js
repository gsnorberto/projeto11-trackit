import { BrowserRouter, Routes, Route } from "react-router-dom";

export default function PrivateRoutes() {
    return (
        <BrowserRouter>
            <Routes>
                <Route />
            </Routes>
        </BrowserRouter>
    );
}